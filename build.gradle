plugins {
  id "com.github.hierynomus.license" version "0.15.0"
  id "com.xebialabs.xl.docker" version "1.1.0"
// Need to figure out how to better use version 10.x of the nebula release plugin.
  id "nebula.release" version "8.0.0"
  id "com.github.ben-manes.versions" version "0.21.0"
}

apply plugin: 'java'
apply plugin: 'idea'
apply plugin: 'eclipse'
apply plugin: 'maven'

xlDocker {
  compileImage = 'xebialabsunsupported/xlr_dev_compile'
  compileVersion = '8.6.2'
  runImage = 'xebialabsunsupported/xlr_dev_run'
  runVersion = '8.6.2'
  runPortMapping = '15516:5516'
}

if (!project.hasProperty('release.scope')) {
  project.ext['release.scope'] = 'patch'
}

if (!project.hasProperty('release.useLastTag')) {
  project.ext['release.useLastTag'] = true
}

license {
  header rootProject.file('License.md')
  strictCheck false
  ext.year = Calendar.getInstance().get(Calendar.YEAR)
  ext.name = 'XEBIALABS'
}
